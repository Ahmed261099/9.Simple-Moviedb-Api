# 9.Simple-Moviedb-Api
This is simple app to get all media trending on day `API`  by using ` XMLHttpRequest()` and implement `open`-`send` methods and then `addeventListener` ("readystatechange") to Checks if `readyState==4` & `status==200` to get results and finally display it by using bootstrap5 grid 
